# Copilot Instructions

## General Guidelines
- First general instruction
- Second general instruction

## Code Style
- Use specific formatting rules
- Follow naming conventions

## Project-Specific Rules
- In `viewLedger.component.ts`, hardcode `availableYears` and update it manually once a year instead of computing it from ledger data.
- Place reusable filtering logic in `LedgerService`, utilizing optional filter parameters rather than required ones.